// file to cerate types for the application

// login type
export type LoginType = {
  email: string;
  password: string;
};

// signup type
export type SignupType = {
  name: string;
  email: string;
  password: string;
};
